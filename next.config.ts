import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "media.licdn.com",
      "static.licdn.com",
      "www.kumarijob.com",
      "merojob.com",
    ],
  },
};

export default nextConfig;
